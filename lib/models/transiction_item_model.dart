class TransictionItemModel {
  final String title, subtitle, trailing;
  final bool isWithDrawal;

  const TransictionItemModel({
    required this.title,
    required this.subtitle,
    required this.trailing,
    required this.isWithDrawal,
  });
}
